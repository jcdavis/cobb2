#include "dline.h"
#include "cobb2.h"
#include "trie.h"

op_result trie_upsert(trie_tx* existing,
                      char* string,
                      unsigned int start,
                      unsigned int total_len,
                      int score,
                      upsert_state* state) {
  
  return NO_ERROR;
}
